# .dotfiles

Configuration files on my local machines. Use `stow` to manage these files.

## Steps

1. Install `stow`: `sudo apt install stow`
1. Clone this repo: `git clone <repo>`
1. `cd .dotfiles`
1. `stow <package>`. `<package>` is a sub-directory: `git`, `emacs`, etc.
